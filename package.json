{
	"name": "verse-twitter-clone",
	"version": "1.0.0",
	"scripts": {
		"heroku-postbuild": "cd frontend && npm install && npm install -g typescript && npm i --save-dev @types/react && npm run build"
	},
	"engines": {
		"node": "18.15.0"
	}
}
